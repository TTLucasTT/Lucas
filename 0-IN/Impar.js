"use strict";
exports.__esModule = true;
var rls = require("readline-sync");
var numIngresado = rls.questionInt("Ingrese un número: ");
while (function (numIngresado) { return 0; }) {
    var numIngresado_1 = rls.questionInt("Ingrese otro número (positivo): ");
}
if (numIngresado == 0 % 2) {
    console.log("El número par");
}
else {
    console.log("El número impar");
}
